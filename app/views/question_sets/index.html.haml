%h1 Listing My Question Sets

%p 
  = @question_sets.length.to_s + ' question sets are available'

%table
  %thead
    %tr
      %th Name
      %th Owner
      %th Last Updated
      %th
      %th
      %th
      %th

  %tbody
    - @question_sets.each do |question_set|
      %tr
        %td= question_set.name
        %td= question_set.user.name
        %td= question_set.updated_at.to_formatted_s(:short)
        %td= link_to 'Show', question_set
        %td= link_to 'Edit', edit_question_set_path(question_set)
        %td= link_to 'View Questions', question_set_questions_path(question_set)
        %td= link_to 'Delete', question_set, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br
%form{method: 'get', action: new_question_set_path}
  %button.button{type: 'submit'} New Question Set
